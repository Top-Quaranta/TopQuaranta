# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg

# Operational scratch dir on prod (one-off CSV exports etc.). Kept out of
# git so it doesn't trip the tq-health git-drift check.
data/

# Virtual environment
venv/
.venv/

# Environment
.env

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Django
*.sqlite3
staticfiles/
media/

# Node
node_modules/
# `package-lock.json` is COMMITTED for reproducible builds — needed by
# CI (`npm ci` + setup-node cache). Do NOT add it back to the ignore
# list. Caught 2026-05-07 when the new frontend-tests CI job failed
# with "Some specified paths were not resolved, unable to cache
# dependencies".

# OS
.DS_Store
Thumbs.db

# Coverage
htmlcov/
.coverage
.coverage.*

# pytest
.pytest_cache/

# ML models (binary, generated locally by recalcular_ml)
music/lid.176.ftz
music/ml_tfidf.joblib
music/ml_model.joblib

# Claude Code
.claude/
.claude.json
# Claude Code personal memory — per-developer, never shared. Distinct
# from the tracked, shared CLAUDE.md project memory (see CLAUDE.md §11),
# which MUST stay committed.
CLAUDE.local.md

# Temporary files
temp/

# Spleeter downloads model weights here when run from project root.
# (Used by scripts/model_comparison/runners/run_spleeter.py.)
pretrained_models/
# Pre-Whisper ML artifacts kept locally for rollback diffing only.
music/ml_*.pre_whisper.joblib
music/ml_directions*.joblib
.secrets/
*.bak
