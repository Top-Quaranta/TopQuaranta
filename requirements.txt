# Install with:
#   pip install --extra-index-url https://download.pytorch.org/whl/cpu -r requirements.txt
# (torch is pinned to the CPU-only build; PyPI doesn't host +cpu wheels.)

django==6.0.6
psycopg2-binary==2.9.12
python-decouple==3.8
dj-database-url==3.1.2
requests==2.34.2
gunicorn==26.0.0
djangorestframework==3.17.1
scikit-learn>=1.9.0
joblib>=1.5.3
# Security
argon2-cffi==25.1.0          # S10: stronger password hashing
django-axes==8.3.1           # S4: brute-force login protection
django-otp==1.7.0            # S11: TOTP 2FA for staff
qrcode[pil]==8.2             # S11: render enrollment QR (pulls Pillow)
# Social renderer — brand logo + territory icons rasterisation. Caught
# 2026-05-07: missing in production meant every territorial portada
# published with an empty pill where the logo should have been.
cairosvg==2.9.0
# Self-hosted cover pipeline (Fase 1, 2026-05-30): Deezer covers
# downloaded + transcoded to webp/avif variants. Pillow is already
# pulled by qrcode[pil]/cairosvg; this adds the AVIF encoder plugin.
# System dep: libavif (apt install libaom-dev libavif-dev) — see
# docs/architecture/portades.md.
pillow-avif-plugin==1.5.5
# Language identification (Whisper LID as a staff signal on Canco)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.0+cpu            # CPU-only PyTorch for faster-whisper backbone
faster-whisper==1.2.1        # large-v3 LID via detect_language(), int8 CPU
soundfile==0.14.0            # WAV loader (no torchcodec dependency)

# SEO monitoring (Sprint S Bloc D, 2026-05-06).
google-api-python-client>=2.197.0
google-auth>=2.54.0
google-auth-httplib2>=0.4.0

# Newsletter → Community bridge (2026-06-10): convert NewsletterDraft
# narrative_html to markdown for the public Publicacio body (option b).
# Pulls beautifulsoup4. The feed renders cos with react-markdown + remark-gfm.
markdownify>=0.13,<1.0
