# Recon TopQuaranta — 2026-06-09

> Fotografia de l'estat real abans de decidir res. **NOMÉS LECTURA.** Cap
> commit, push, PR ni escriptura al repo; només consultes de lectura contra
> prod (Hetzner, Django shell amb `settings.production`). Aquest fitxer és una
> nota local de referència, **NO** forma part del repo.
>
> Generat: 2026-06-09. Prod `HEAD = origin/main = 56a43f4` (#196).

---

## 1. Alineació git / prod — RESOLT, sense drift

**Conclusió: el drift de la guarda de caducitat ja està resolt. Prod i GitHub
estan alineats.**

- La branca **`fix/create-track-caducitat-guard` es va mergejar** com a
  **PR #131** (`021bf03`, *"fix(ingesta): DIES_CADUCITAT guard at every
  Canco-creation frontier"*, merged **2026-06-02 12:07 UTC**). El follow-up
  d'enriquiment és **PR #141** (`aac3999`, *"exclude caducats from the enrich
  pending pool"*, merged **2026-06-03**). Tots dos són ancestres d'`origin/main`.
- `ingesta/caducitat.py` i `ingesta/tests/test_caducitat_guard.py` **són a
  l'arbre d'`origin/main`** (verificat amb `git ls-tree`). Les edicions
  `is_caducat` viuen a `obtenir_novetats._create_track` (i `obtenir_metadata`).
- **Prod:** `cd /home/topquaranta/app && git rev-parse HEAD` →
  `56a43f4` == `origin/main`. `git status` net (excloent `data/`).
  `ingesta/caducitat.py` present (datat Jun 3). **No hi ha drift.**
- Local: `main` net i sincronitzat amb `origin/main` (`git status` net tret de
  fitxers `.md`/`.html` no-tracked al working dir).

> El caveat del doc `platform-overview.md` (§3 línies 207-211 i §9 línies
> 425-434) que deia "deployed but uncommitted, prod ahead of GitHub" **ja és
> fals** — vegeu §5 d'aquest recon.

Detall de la guarda (`ingesta/caducitat.py`): `is_caducat(data, cutoff)` →
`data is not None and data < cutoff`. **NULL → False** (no caduca, per disseny;
mateixa semàntica que `netejar_caducades` amb `__lt`). El forat dels NULL
latents continua sent un follow-up conegut documentat però **avui no actiu**
(0 files; vegeu §2).

---

## 2. Números frescos de prod (lectura) vs doc 2026-06-02

| Mètrica | 2026-06-02 (doc) | 2026-06-09 (avui) | Canvi |
|---|---|---|---|
| **Canco.pendents() total** | **784** | **341** | ▼ −443 (−56 %) |
| └ per ml_classe | C=651, A=84, B=49 | **C=325, B=16, A=0** | A desapareix; C ▼; B ▼ |
| └ per territori (artista principal) | CAT 437, VAL 84, BAL 28, sense-territori 235 | **CAT 234, VAL 44, BAL 23, None 40** | "sense artista aprovat" cau de 235 → 40 |
| **HistorialRevisio (rebuigs) total** | 12 264 `rebutjada` | **15 608 files totals** | ▲ rebuigs actius |
| **data_llancament NULL latents** | 0 (Canco), latent | **0 Canco actives, 0 Albums no-descartat** | igual (forat latent, no actiu) |

**Per què cau la cua de pendents 784 → 341:**

1. **PR #159** (*"cançons pendents han de tindre ≥1 artista aprovat per ser
   revisables"*, 2026-06-07) — redefineix què compta com a pendent revisable;
   això explica que el bucket "sense territori / artista no aprovat" passi de
   235 a 40.
2. **PR #131/#141** guarda de caducitat — para la recreació de pendents
   ancians cada ~30 dies.
3. **PR #161** (*escombrat recurrent d'orfes*) + **#134** (tombstone pendents
   on discard) — higiene contínua de la cua.

**Cua d'ingesta / enriquiment Spotify (cobertura):**

- Pool canònic = `Canco(activa=True, verificada=True)` amb ISRC → **3 463**
  (el 100 % de les actives+verificades tenen ISRC).
- `SpotifyMetadata`: 5 214 files. `enrichment_status`:
  `found` 2 821 · `not_attempted` 2 371 · `not_found` 15 · `manual` 7.
- **Cobertura enriquida (found+manual) sobre el pool actiu = 2 088 / 3 463 =
  60,3 %.** Pendents d'enriquir: `not_attempted` 1 331 + sense-fila 38.
- Excloent caducats (el pool real de `enriquir_spotify` post-#141): 2 828
  cançons; enriquides 1 521 (53,8 %); **pendents reals ≈ 1 301**
  (1 263 `not_attempted` + 38 sense-fila). *Nota:* la cobertura BAIXA en
  excloure caducats → molts dels "enriquits" són tracks vells/caducats.
- KPI oficial: `web/api/staff/estat.py:440 _spotify_enrichment_stats()` exposa
  `coverage_total` / `coverage_public` / `coverage_pending`
  (`found = enrichment_status ∈ LOCKED_STATUSES`).
- Cua d'ingesta Deezer: **0** albums no-descartats amb `deezer_id` i
  `last_album_check=NULL` (mai comprovats) → el cooldown P2 està al dia.
  Albums no-descartats totals: 2 450.

---

## 3. Inventari del sistema intern de "publicacions" (comunitat)

Mapa de models / endpoints / superfícies (read-only; res tocat). App `comptes`
+ `web/api/comunitat_views/` + SPA `web-react/src/pages/`.

| Superfície | Models (file:line) | Endpoints | SPA | Estat |
|---|---|---|---|---|
| **Registre d'usuaris** | `Usuari` (comptes/models.py:18), `PerfilUsuari` (263) | `POST /api/v1/auth/register/` (auth_views.py:185), `/auth/me/`, `/auth/login/` | `AuthPage.jsx` | **Complet** (activació per email, consentiment RGPD, opt-in newsletter) |
| **Missatgeria / DM** | `Missatge` (comptes/models.py:443) | `/api/v1/missatges/`, `/missatges/nou/`, `/missatges/amb/<pk>/` (comunitat_views/missatgeria.py) | `MissatgesPage.jsx` | **Complet** (1-a-1, no threading, throttle d'enviament) |
| **Admin pseudo-user + DM relay** | seed `Usuari(username="admin")` via migració `comptes.0016`; `ADMIN_INBOX_USERNAME` | reutilitza `missatge_crear`; fan-out per email a tot staff via `_enviar_notificacio_missatge` | (apareix al directori) | **Implementat** (per CLAUDE.md; **NO és un model nou** — l'agent de codi no el va trobar perquè és constant+seed+fan-out email, no taula) |
| **Cerca d'usuaris / directori** | `PerfilUsuari` (263), camp `visible_directori` | `GET /api/v1/comunitat/directori/` (perfil.py:104) | `ComunitatDirectoriPage.jsx` | **Complet** (staff veu TOTS els perfils per moderació; cerca per nom/instrument/bio + filtres rol/territori/colab) |
| **Comptes d'artista** | `UserArtista` (comptes/models.py:32), `PropostaArtista` (103) | `/compte/propostes/`, `/compte/solicituds/`, `/compte/artista/<pk>/editar/`; staff `/staff/propostes/`, `/staff/solicituds/` | `ProposarArtistaPage.jsx`, `SolicitarGestioPage.jsx`, `ComptePage.jsx`, staff `PropostesPage`/`SolicitudsPage` | **Complet** (flux proposta→aprovació→gestor verificat→auto-edició; camps d'auditoria Fase 1.5; emails) |
| **Publicacions públiques / feed** | `Publicacio` (comptes/models.py:371), `Comentari` (479) | `/api/v1/comunitat/publicacions/` (GET/POST), `/publicacions/<pk>/`, **`/publicacions-publiques/` (sense auth)**, comentaris; staff `/staff/publicacions/` + `/decidir/` | `ComunitatPage.jsx`, **`ComunitatPublicaPage.jsx` (PÚBLICA)**, `ComunitatPublicarPage.jsx`, staff `StaffPublicacionsPage.jsx` | **Complet** (markdown+imatge; `visibilitat ∈ {interna,publica}` × `estat ∈ {esborrany,pendent,publicat,rebutjat}`; moderació staff: directe; usuari públic: pendent) |

### Pregunta crítica: pot la newsletter publicar-se com a Publicacio pública aquí?

**NO — avui és inexistent (cap fontanera).** La newsletter i les Publicacions
són sistemes separats:

- Newsletter = només email. Model `NewsletterDraft` (comptes/models.py:608):
  `subject` + `narrative_html` + `estat ∈ {pendent,enviat,cancellat}`. **Cap FK
  a `Publicacio`.** Enviament via `comptes/newsletter.py` (`EmailMultiAlternatives`).
  Audit via `SocialPost(platform=newsletter)`.
- Endpoints rutina (`web/api/newsletter_routine.py`: `/brief/`, `/esborrany/`)
  i staff (`web/api/staff/newsletter.py`) només toquen `NewsletterDraft`. Cap
  crea `Publicacio`. `grep "Publicacio.*newsletter"` → 0 resultats.
- Per fer-ho caldria: (a) camp/FK `NewsletterDraft → Publicacio`, (b) endpoint
  o acció staff "publicar com a post comunitari", (c) botó a l'editor staff.

> **Viabilitat:** és una passarel·la petita i neta de construir (el destí
> `Publicacio(visibilitat=publica, estat=publicat)` ja existeix i ja té pàgina
> pública `/comunitat/public`). Avui, però, **no hi ha cap línia de codi** que
> ho faci.

---

## 4. Sonda Crim / Spotify — split VIABLE però no 100 % automàtic avui

**Sí, guardem el Spotify artist_id per cançó.** A `SpotifyMetadata`
(music/models.py:1525): `spotify_id`, **`spotify_artist_id`**,
`spotify_artist_ids` (plural, multi-artista), `spotify_artist_name`,
`enrichment_status`, `enriched_at`. (No a `Album`; `Album.spotify_id` i
`Artista.spotify_id` són columnes dormants pre-split, poc usades.)

**L'únic Crim aprovat: `Artista pk=2908` ("Crim"), `Deezer 347962`, 5 albums.**
9 cançons (principal+col·laborador). Reparteix així per Spotify artist_id:

| Cançó | Àlbum | data | ISRC (prefix) | Spotify artist_id | status |
|---|---|---|---|---|---|
| Futur Medieval | Futur Medieval | 2025-10-16 | QMDA6 | **2p7rRgVQsbzdA8zkpb83Q2 "Crim"** | found |
| Combats d'Autoesgrima | Futur Medieval | 2025-10-16 | QMDA6 | **2p7rRgVQsbzdA8zkpb83Q2 "Crim"** | found |
| Res de Nou | Futur Medieval | 2025-10-16 | QMDA6 | **2p7rRgVQsbzdA8zkpb83Q2 "Crim"** | found |
| Newpe$o | Newpe$o | 2026-01-23 | QZES9 | **01LXoxJ5WJSPQ1w7zDLm9j "crim"** | found |
| Carnets de Punk | Carnets de Punk | 2025-04-24 | DEVF4 | *(buit)* | found |
| Presó Mental | Carnets de Punk / Presó Mental | 2025-05-06 | DEVF4 | *(buit)* | found |
| Eye Cant Cry | Eye Cant Cry | 2026-05-22 | QZYHJ | *(sense fila SpotifyMetadata)* | — |
| Paper Cuts | Paper Cuts | 2026-05-25 | QZYHJ | *(sense fila)* | — |
| Sugar Daddy (Demo) | Sugar Daddy (Demo) | 2026-06-01 | QZYHJ | *(sense fila)* | — |

**Lectura del split:** el Deezer `347962` **conflateix com a mínim dos
artistes reals**:
- **Crim (punk català)** → Spotify `2p7rRgVQsbzdA8zkpb83Q2` ("Crim"): àlbum
  *Futur Medieval*. Probablement també *Carnets de Punk* / *Presó Mental*
  (títols catalans; artist_id buit però registrant DEVF4).
- **"crim" (un altre, minúscula)** → Spotify `01LXoxJ5WJSPQ1w7zDLm9j`: *Newpe$o*
  i, gairebé segur, els 3 singles anglesos 2026 (*Eye Cant Cry*, *Paper Cuts*,
  *Sugar Daddy (Demo)*) — mateix registrant **QZYHJ**, títols en anglès,
  encara **no enriquits**.

**Viabilitat de separar els dos Crim:** **alta, però NO purament automàtica
avui.** Dels 9 tracks, només **4 porten Spotify artist_id**, i aquests 4
parteixen net 3-vs-1 entre els dos ids. Però:
- 3 tracks (singles QZYHJ) **no tenen fila SpotifyMetadata** → cap clau Spotify.
- 2 tracks (DEVF4) tenen `status=found` però `spotify_artist_id` buit.

**Camí recomanat (quan es decideixi actuar):**
1. Primer **enriquir** (manual hydrate o `enriquir_spotify`) els 5 tracks sense
   artist_id perquè tots portin Spotify artist_id.
2. Partir per `spotify_artist_id`: el Crim català manté `Deezer 347962` +
   Spotify `2p7r...`; l'altre "crim" surt a un `Artista` nou amb Spotify
   `01LX...`. **Atenció a l'invariant** "aprovat ⇒ Deezer ID OR MBID": el nou
   Crim necessitarà àncora pròpia (buscar el seu Deezer/MBID) abans d'aprovar.
3. ISRC registrant és un bon senyal secundari (QMDA6+DEVF4 → català;
   QZES9+QZYHJ → l'altre).

**Cap split ni escriptura feta. Només lectura.**

---

## 5. Doc desfasat — `docs/platform-overview.md` (verificat 2026-06-02)

Afirmacions que **ja no són certes** o han quedat incompletes (el doc es va
escriure el 2026-06-02; des d'aleshores s'han mergejat PRs #134–#196):

1. **§3 línies 207-211 (caveat repo-state) + §9 línies 425-434 ("Caducity guard
   deployed but uncommitted, prod ahead of GitHub")** → **FALS.** La guarda es
   va mergejar via **PR #131** (2026-06-02) + **#141** (2026-06-03). `origin/main`
   ja conté `ingesta/caducitat.py`; prod == GitHub. **Tota aquesta secció s'ha
   d'eliminar.**

2. **§6 "Per-song editing" — "There is currently no Spotify field in the song
   edit form" i tota la llista de backlog de 3 capes** → **FALS.** **PR #139**
   (*"manual Spotify id with /search-free hydration"*, 2026-06-02) ho va
   implementar: `web/api/staff/cancons.py:417` accepta `spotify_url` al PATCH,
   amb hidratació id-first (estats `ok`/`pending`/`failed`) i
   `_spotify_url.parse_track_id`. El "backlog" descrit ja està fet.

3. **§9 Diagnòstic "per verificar" — totes les xifres (784 pendents;
   C=651/A=84/B=49; CAT 437/VAL 84/BAL 28/235-sense-territori; 12 264 rebuigs;
   4 309 en 30 dies)** → **DESFASAT.** Avui: 341 pendents; C=325/B=16/A=0;
   CAT 234/VAL 44/BAL 23/None 40; 15 608 files HR. A més, **PR #159** va canviar
   la semàntica (un pendent ara requereix ≥1 artista aprovat per ser revisable),
   cosa que invalida la frase "235 whose artist isn't approved yet".

4. **§7 línia 395 "territorial redesign stays the future Step 3c"** → **FALS.**
   **PR #146** (*territorial editorial story set, Step 3c*, 2026-06-04) ja es va
   fer; **#148** va treure el path mort `render_stories_top`.

5. **§7 Social com a cron fix esglaonat** → **INCOMPLET / superat.** Tot un
   subsistema nou de **distribució** s'ha afegit: matriu editable canal×tipus +
   interruptor mestre (PRs **#163, #169, #173, #174, #180, #181, #184, #189,
   #195**), Instagram com a ChannelView de 4 seccions, gate `dia_setmana` per
   cel·la. El model "cron staggered fix" del §7 ja no descriu la realitat.

6. **§8 "Recent relevant PRs"** s'atura a #129 + "caducity guard" → **OBSOLET.**
   No reflecteix l'sprint de juny (#134–#196): guarda caducitat mergejada,
   enriquiment Spotify per prioritats (#186), KPI de cobertura a `estat`
   (#182), gate d'aprovació Deezer obligatori (#185), `editorial_veu` prompt a
   `ConfiguracioGlobal` (#187/#193, servit al brief), newsletter routine +
   preview + opt-out (#164–#168, #176, #177, #192), soft-cap adaptatiu per
   territori off-by-default (#196), etc.

7. **Cobertura / KPI** — el doc no esmenta el KPI catalog-wide d'enriquiment
   (`estat.py:440`, PR #182). Falta, no fals.

8. **`editorial_veu` a `ConfiguracioGlobal`** (ranking/models.py:245, migracions
   0021/0024; servit al brief de newsletter) — concepte nou que el doc no
   recull. Falta, no fals.

> **Recomanació (quan es decideixi):** actualitzar la capçalera "Last verified"
> i, com a mínim, refer §3 caveat, §6, §7, §8 i §9 amb les xifres d'aquest
> recon. Eliminar tota referència a "prod ahead of GitHub".

---

## Apèndix — com s'han tret els números (reproduïble, read-only)

```bash
# Prod git
cd /home/topquaranta/app && git rev-parse HEAD origin/main && git status --porcelain | grep -v '^?? data/'

# Django shell (settings explícit — sudo no passa l'env)
sudo -u topquaranta env DJANGO_SETTINGS_MODULE=topquaranta.settings.production \
  .venv/bin/python manage.py shell
#   Canco.objects.pendents().count() / .values('ml_classe')...
#   SpotifyMetadata.objects.values('enrichment_status').annotate(...)
#   Artista.objects.get(pk=2908)  # Crim
```

Cap comanda d'escriptura executada. Cap fitxer del repo modificat. Cap PR obert.
